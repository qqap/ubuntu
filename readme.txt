just setup a user "a" in a ubuntu system

i tried making my own linux distro, called "towel", but 
git is sooo hard to setup, especially since it cannot just be a static binary.
so, i just use ubuntu for now, which just works.

pain.